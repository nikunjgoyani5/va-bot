import 'package:flutter/material.dart';

class LeadsFollowUpWidget extends StatelessWidget {
  const LeadsFollowUpWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Leads & Follow Up Widget'),
    );
  }
}
