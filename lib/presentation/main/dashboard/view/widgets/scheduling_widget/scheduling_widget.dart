import 'package:flutter/material.dart';

class SchedulingWidget extends StatelessWidget {
  const SchedulingWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Scheduling Widget'),
    );
  }
}
