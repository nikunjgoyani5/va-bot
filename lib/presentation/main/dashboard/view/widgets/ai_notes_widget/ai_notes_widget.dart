import 'package:flutter/material.dart';

class AINotesWidget extends StatelessWidget {
  const AINotesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('AI Notes Widget'),
    );
  }
}
